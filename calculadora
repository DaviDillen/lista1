#include <stdio.h>
#include <stdlib.h>

int main(){
    float valor1;
    float valor2;

    scanf("%f", &valor1);
    scanf("%f", &valor2);
    printf("soma: %f\n", valor1 * valor2);
    divisao = valor1 / valor2;
    printf("%f", divisao);
    return 0;
}
